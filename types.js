export const ClientStatus = Object.freeze({
  ACTIVE: 'active',
  INACTIVE: 'inactive',
});

export const TicketType = Object.freeze({
  PRIVATE: 'private',
  GROUP: 'group',
});

export const PackTemplateStatus = Object.freeze({
  ACTIVE: 'active',
  INACTIVE: 'inactive',
});

export const PackHistoryType = Object.freeze({
  PURCHASE: 'purchase',
  ATTENDANCE: 'attendance',
  EXPIRY_CHANGE: 'expiry_change',
});

export const Page = Object.freeze({
    DASHBOARD: 'dashboard',
    CLIENTS: 'clients',
    TEMPLATES: 'templates',
    ATTENDANCE: 'attendance',
});
